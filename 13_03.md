# Задания 0
## 
```
n = int(input()) #Вводится любая переменная.
r = bin(n) #Выводит переменную n в двоичную систему.
print(r) #Вывод числа.
```
## 
```
n = int(input()) #Вводится любая переменная.
r = bin(n) [2:] #Выводит переменную n в двоичную систему.
print(r) #Вывод числа.
if n % 2 == 0: #Если число четное то выведи 01
    r += '01'
else: #Если число не четное выведи 10
    r += '10'
print(r)
```
## 
```
for n in range(1, 100):
    r = bin(n) [2:] #Выводит переменную n в двоичную систему.
    if n % 2 == 0: #Если число четное то выведи 01
        r += '01'
    else: #Если число не четное выведи 10
        r += '10'
    print(n, r)
```
# Задания 1
## 
```
n = int(input()) #Вводится любая переменная.
r = bin(n)[2:] #Выводит переменную n в двоичную систему.
print(r)
if r.count('1') % 2 == 0: #Если число четное добавь число 0
    r += '0'
else: #Если число нечетное добавь 1 
    r =+ '1'
print(r)
if r.count('1') % 2 == 0:
    r += '0'
else:
    r += '1'
print(r) #Выведи число 
```

# Задание 2
##
```
x = 24 # x заданное число (24)
for i in range(1, x+1): # Добавляя один включая число 24.
    if x % i == 0: # Если 
        print(i) # Выведи число.
```
##
```
x = 240 # Заданное число
c = 0 # Заданное число c 
for i in range(1, x+1): # 
    if x % i == 0:
        c +=1
print(c)
```
##
```
for x in range(126849, 126872):
    c = 0
    for i in range(1, x+1):
        if x % i == 0:
            c += 1
    if c == 4:
        print(x)
```
##
```
for x in range(126849, 126872):
    c = 0
    for i in range(1, x+1):
        if x % i == 0:
            c += 1
            print(i)
    if c == 4:
        print(x)
```
##
```
for x in range(126849, 126872):
    c = set()
    for i in range(1, x+1):
        if x % i == 0:
            c.add(i)
    if len(c) == 4:
        print(c)
```
